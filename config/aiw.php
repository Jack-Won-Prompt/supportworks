<?php

return [

    /*
    |---------------------------------------------------------------------------
    | 작업 PC(데몬) 온라인 판정
    |---------------------------------------------------------------------------
    | last_seen_at 이 이 초 이내면 온라인으로 본다. 데몬의 HEARTBEAT_SEC(기본 30)
    | 보다 넉넉해야 한 번 놓쳤다고 오프라인으로 뜨지 않는다.
    */
    'offline_after_sec' => env('AIW_OFFLINE_AFTER_SEC', 90),

    /*
    |---------------------------------------------------------------------------
    | 토큰
    |---------------------------------------------------------------------------
    */
    'token_ttl_days' => env('AIW_TOKEN_TTL_DAYS', 90),

    /*
    |---------------------------------------------------------------------------
    | 비용 상한 (USD)
    |---------------------------------------------------------------------------
    | job 단위 누적 비용 상한. 초과하면 서버가 job 을 failed 로 강제 전이하고
    | 데몬에 취소를 발행한다.
    |
    | 기본은 "상한 없음" 이다. 담당자 PC 는 구독 로그인으로 돌아서 화면에 찍히는
    | 금액은 실제 청구가 아니라 환산값이다. 상한을 걸어도 돈은 그만큼 아껴지지
    | 않고, 사람이 없는 시간에 작업만 중간에서 끊긴다 — 되살리려면 결국 사람이
    | 다시 눌러야 한다. 폭주를 막는 것은 금액이 아니라 담당자 PC 의 시간 제한이다
    | (실행 30분 / 무응답 5분 / 총 2시간).
    |
    | 아래 금액은 사람이 화면에서 "제한 없음" 을 끄는 순간 칸에 채워지는 값이다.
    */
    'default_no_cost_limit'  => env('AIW_DEFAULT_NO_COST_LIMIT', true),
    'default_cost_limit_usd' => env('AIW_DEFAULT_COST_LIMIT_USD', 2.00),

    /*
    |---------------------------------------------------------------------------
    | 운영 오류 수집
    |---------------------------------------------------------------------------
    | 운영 사이트가 웹훅으로 보내오는 예외를 받는 쪽 설정이다.
    |
    | error_intake_per_minute
    |   출처 하나가 1분에 보낼 수 있는 건수. 같은 에러는 지문으로 묶여 한 행에
    |   쌓이지만, 요청 자체는 그대로 들어와 DB 를 때린다. 운영 장애 때 초당
    |   수백 건이 오는 순간이 정확히 그렇다.
    |
    | error_patch_max_attempts
    |   한 지문에 대해 자동으로 작업 지시를 만들 수 있는 횟수. 고친 코드가 또
    |   에러를 내면 웹훅 → 새 지시 → 또 에러로 끝없이 돈다. 사람이 없는 시간에
    |   그 고리가 돌면 아무도 멈추지 못하므로 횟수로 끊는다.
    */
    'error_intake_per_minute'   => env('AIW_ERROR_INTAKE_PER_MINUTE', 120),
    'error_patch_max_attempts'  => env('AIW_ERROR_PATCH_MAX_ATTEMPTS', 2),

    /*
    |---------------------------------------------------------------------------
    | 승인 요청 타임아웃 (분)
    |---------------------------------------------------------------------------
    | 이 시간이 지나도 결정되지 않은 승인 요청은 스케줄러가 expired 로 바꾸고
    | 데몬에 deny("timeout") 를 보낸다.
    */
    'permission_timeout_min' => env('AIW_PERMISSION_TIMEOUT_MIN', 30),

    /**
     * 사람의 답을 기다린 지 이만큼 지나면 다시 알린다(분). 이 간격마다 한 번씩,
     * nudge_max 번까지. 0 으로 두면 재알림을 끈다.
     */
    'nudge_after_min' => env('AIW_NUDGE_AFTER_MIN', 10),
    'nudge_max'       => env('AIW_NUDGE_MAX', 3),

    /**
     * 환경 때문에 끊긴 작업(담당자 응답 없음·데몬 재시작)을 서버가 스스로 다시
     * 보내는 횟수. 0 이면 끈다. 비용·시간 상한이나 코드 문제로 인한 실패는
     * 여기 해당하지 않는다 — 그대로 다시 보내면 같은 자리에서 또 멈춘다.
     */
    'auto_retry_max' => env('AIW_AUTO_RETRY_MAX', 1),

    /**
     * 자동 배포가 실패했을 때 돌릴 운영 명령의 이름. 그 이름으로 등록된
     * 운영 명령이 있으면 실행해 결과를 남긴다. 없으면 아무 일도 하지 않는다.
     */
    'health_check_name' => env('AIW_HEALTH_CHECK_NAME', '점검'),

    /**
     * 커밋·푸시나 배포가 이만큼(분) 실행 중인 채로 진전이 없으면, 받아 간 데몬이
     * 사라진 것으로 보고 따라잡기 목록에 다시 올린다. 다시 돌려도 안전하다 —
     * 이미 합쳐진 커밋은 publish 쪽이 그렇게 판정한다.
     */
    'artifact_stale_min' => env('AIW_ARTIFACT_STALE_MIN', 10),

    /*
    |---------------------------------------------------------------------------
    | 툴 화이트리스트
    |---------------------------------------------------------------------------
    | 서버가 강제한다. 웹에서 들어온 allowed_tools 가 이 집합을 벗어나면 거부한다.
    | auto_approvable 은 permission_mode=acceptEdits 일 때 자동 승인되는 툴이다.
    |
    | Bash 포함은 의식적인 선택이다(운영자 결정, 2026-09-11). acceptEdits 를 고른
    | job 은 임의 명령을 사람 확인 없이 실행한다. 이 모드에서 남는 방어선은
    | 데몬의 샌드박스 차단 목록(git push·광범위 삭제·자격증명 접근 등)과 작업
    | 폴더 경계뿐이며, 둘 다 셸 우회를 전부 막지는 못한다.
    |
    | 매번 확인이 필요한 작업은 permission_mode=default 로 등록한다 — 그쪽은
    | 이 목록과 무관하게 모든 툴이 승인을 거친다.
    */
    'supported_tools' => ['Read', 'Edit', 'Write', 'Bash', 'Glob', 'Grep', 'WebFetch', 'WebSearch'],
    'default_tools'   => ['Read', 'Edit', 'Write', 'Bash', 'Glob', 'Grep'],
    'auto_approvable' => ['Read', 'Edit', 'Write', 'Bash', 'Glob', 'Grep'],

    /*
    |---------------------------------------------------------------------------
    | 컨텍스트 한도 (모델별)
    |---------------------------------------------------------------------------
    | job 등록 시 선택한 모델의 값을 aiw_jobs.context_limit_tokens 에 복사한다.
    | 데몬이 이 값의 CONTEXT_HANDOVER_RATIO 배에 도달하면 인수인계를 트리거한다.
    */
    'default_context_limit_tokens' => 200_000,
    'model_context_limits' => [
        // 'claude-opus-5' => 1_000_000,
    ],

    /*
    |---------------------------------------------------------------------------
    | 저장 정책
    |---------------------------------------------------------------------------
    | DB 비대를 막는다. raw 는 잘라 저장하고 큰 diff 는 파일로 옮긴다.
    */
    'log_raw_max_bytes'   => 4 * 1024,
    'git_diff_max_inline' => 200 * 1024,
    'retention_days'      => env('AIW_RETENTION_DAYS', 90),
];
